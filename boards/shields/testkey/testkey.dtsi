/ {
    key_matrix {
        compatible = "zmk,matrix-transform";
        columns = <1>;
        rows = <1>;
        col-gpios = <&gpio0 10 (GPIO_ACTIVE_LOW)>;
        row-gpios = <&gpio0 12 (GPIO_ACTIVE_LOW)>;
    };
};
